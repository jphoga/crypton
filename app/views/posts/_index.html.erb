<% @posts.each do |post| %>
  <%= render 'posts/individual', post: post %>
<% end %>
<script>
  function triggerComment(button) {
    button.parentNode.parentNode.nextElementSibling.classList.toggle("home_post_comment");
    let form = button.parentNode.parentNode.nextElementSibling;
    console.log(22, form);
   form.style.display == "none" ? "block" : "none";
  }

  function formGone(form) {
    console.log('works');
    console.log(form.parentNode);
    let parent = form.parentNode;
    parent.style.display="none";
  }
</script>
