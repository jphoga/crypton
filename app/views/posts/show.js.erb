// var viewLinks = document.querySelectorAll(".open-post-button");
// viewLinks.forEach((button) => {
// console.log("start JS");
//   button.addEventListener("click", (event) => {
//     console.log(event.currentTarget);
//     var postsContainer = document.querySelector('.post-partial-container');
//     postsContainer.innerHTML = `<%= j render 'posts/show', post: @post, comment: @comment %>`;
//   })
// })

var postsContainer = document.querySelector('.post-partial-container');
postsContainer.innerHTML = '<%= j render 'posts/show', post: @post, comment: @comment %>';
