function refreshVoteButtons(innerHTML) {
  const voteButtons = document.getElementById('voteButtons');
  voteButtons.innerHTML = innerHTML;
}
refreshVoteButtons('<%= j render "posts/vote_buttons", post: post %>');
