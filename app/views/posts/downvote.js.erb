function refreshVoteButtons(innerHTML, id) {
  const voteButtons = document.getElementById('voteButtons-' + id);
  voteButtons.innerHTML = innerHTML;
}
refreshVoteButtons('<%= j render "vote_buttons", post: @post %>', '<%= @post.id %>');
