<%= simple_form_for([post, comment], remote: true, html: { onsubmit: "formGone(this)"}) do |f| %>
  <%= f.input :content, label: false %>
  <%= f.submit 'Submit', class: 'comment-submit-button' %>
<% end %>

